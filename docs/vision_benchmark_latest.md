# Vision benchmark — latest run

This file is **created or overwritten** by `scripts/benchmark_vision.py` on the machine where you run it (e.g. Jetson).

Run:

```bash
cd ~/ugv_ws && python3 scripts/benchmark_vision.py --compare-auto --iterations 200 --input_size 480
```

See also [`vision_benchmark_results.md`](vision_benchmark_results.md) for archived tables and notes.
