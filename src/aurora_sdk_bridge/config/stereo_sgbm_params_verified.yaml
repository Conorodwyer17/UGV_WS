# Phase E: SGBM parameters (verified by sweep or default)
# Target: >= 25% valid disparity in central 60% ROI, compute_time_ms < 75

min_disparity: 0
num_disparities: 64   # 16*4, must be divisible by 16
block_size: 5
P1: 600               # 8*3*block_size^2
P2: 2400              # 32*3*block_size^2
uniqueness_ratio: 10
speckle_window_size: 100
speckle_range: 32
disp12_max_diff: 1
